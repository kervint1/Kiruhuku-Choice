import 'package:flutter/material.dart';

class RegisterclothesScreen extends StatelessWidget {
  const RegisterclothesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("data"),
    ));
  }
}
